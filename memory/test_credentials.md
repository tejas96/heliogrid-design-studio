# Existing POC login (unchanged)
- Authentication is **MOCKED** in the existing `screens/Login.tsx`.
- Phone for browser testing: `9876543210`.
- Password for browser testing: `studio-test` (any non-empty password is accepted by the existing POC).
- No real account or backend credential was created or changed.
- Project persistence is the existing browser storage, not a new backend.